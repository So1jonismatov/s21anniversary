# API Configuration
# This is used by the API proxy route to forward requests to the actual API
# This allows HTTPS Vercel app to communicate with HTTP APIs (avoids mixed content)
NEXT_PUBLIC_API_BASE_URL=http://161.35.204.26:3010
NEXT_PUBLIC_API_ENDPOINT=/api

# Supabase Configuration (if using Supabase for data persistence)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key